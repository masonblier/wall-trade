<%= form_for @wallpaper do |f| %>

  <% if @wallpaper.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@wallpaper.errors.count, "error") %> prohibited
        this wallpaper from being saved:
      </h2>
      <ul>
        <% @wallpaper.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :image %><br>
    <%= f.file_field :image, accept: 'image/png,image/jpeg' %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
