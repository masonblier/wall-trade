<h1>Add Wallpaper</h1>

<%= react_component 'WallpaperForm', {
    data: @wallpaper,
    errors: @wallpaper.errors,
    all_tags: @all_tags,
    form: {
        mode: 'new',
        token: form_authenticity_token.to_s
      }
    } %>

<div class="actions">
  <%= link_to 'Top', wallpapers_path %>
</div>
